# incremental-example

```hs
> example
Just (PersonDiff {diffName = Just "", diffAge = Nothing})
Just (PersonDiff {diffName = Just "", diffAge = Just 14})
Just (PersonDiff {diffName = Just "", diffAge = Just 14})
Just (PersonDiff {diffName = Nothing, diffAge = Nothing})
```
